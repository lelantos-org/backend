target/
# ~200MB of circuit artifacts (zkey/r1cs/wasm). Never baked into an image —
# the relayer mounts them at runtime from stack/circuits/ → /circuits:ro.
stack/circuits/
.git/
.github/
.claude/
.idea/
.vscode/
docs/
PLAN.md
README.md
*.md
